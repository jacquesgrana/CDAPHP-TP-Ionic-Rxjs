import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tp_Ionic_Rxjs',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
